import { AppInstanceExtended } from './appinstanceextended';

describe('AppInstanceExtended', () => {
  it('should create an instance', () => {
    expect(new AppInstanceExtended()).toBeTruthy();
  });
});
